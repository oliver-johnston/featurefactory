id: ui_engineer
name: "UI Engineer"
role: "Frontend Developer"
description: "Builds UI components, implements designs, and integrates APIs."
prompt: "prompt.md"
avatar: "avatar.svg"
capabilities:
  git: true
  filesystem: true
  network: false
defaults:
  shell: bash
  start_status: in_progress
  command:
    - claude
    - --allowedTools
    - Read,Write,Edit,MultiEdit,Glob,Grep,Bash
